# Changelog

## Unreleased

- Activity now opens as a **full center-channel view** at `/:team/activity` (same footprint as chat / Threads), with desktop-matching Activity UI (header, search box, feed rows, hide ×, load more) instead of a right-side drawer.
- Added **Activity** tab in the LHS sidebar (desktop parity: mentions, threads, reactions via `posts/search`, DMs/GMs, reminders with 404 soft-fallback), including **search**, **hide**, **refresh**, and **load more**.
- Hid native **Threads** sidebar entry and the channel header **Mentions** button in this fork (always-on).

