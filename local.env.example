# Local Mattermost API target for webpack-dev-server proxy.
# Copy to local.env and fill in. Never commit local.env.

# Absolute URL of the Mattermost/Time server (API + cookies).
# Used only for local `npm run dev-server:remote`.
MM_SERVICESETTINGS_SITEURL=https://your-mattermost.example.com
