// Stub for local dev when focalboard is not cloned.
import type {ProductPlugin} from '../plugins/products';

export default class BoardsProductStub implements ProductPlugin {
    initialize() {}
    uninitialize() {}
}
